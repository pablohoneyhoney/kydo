{
  "name": "kydo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A bot that listens to a live conversation and serves contextual questions on a screen. For Pablo Honey × Robert Hodgin, Automattic Noho, May 21 2026.",
  "scripts": {
    "dev": "vite --port 5174 --strictPort",
    "server": "node server.js",
    "start": "concurrently -k -n api,vite -c yellow,cyan \"npm run server\" \"npm run dev\"",
    "build": "vite build",
    "preview": "vite preview --port 5174"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "multer": "^2.1.1"
  }
}
